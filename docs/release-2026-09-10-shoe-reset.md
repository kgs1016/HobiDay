# 시작 암벽화 초기화 릴리스 — 2026-09-10

잘못 고른 시작 암벽화는 내 프로필의 ‘시작 색 초기화’에서 9색 중 다시 선택할 수 있다. 적용 기간 안에 계정당 한 번 허용하며, 새 선택일부터 3개월 기간을 새로 시작한다. 최근 3개월의 실제 완등 기록·성취 점수·최소 H난이도 조건은 그대로 유지한다. 실제 성취 단계가 더 높으면 그 단계를 표시한다.

같은 색으로 저장을 재시도하면 기존 결과를 반환하고 기간을 연장하지 않는다. 다른 색으로 반복 초기화하거나 적용 기간 종료 후 초기화하는 것은 거절한다. 취소하면 기존 시작 색을 유지한다. 기준표 링크는 본문색과 굵은 글씨로 표시한다.

## 검증

- 기준 main `c49a5ec`, 보존 커밋 `2d5be66`. 별도 `/private/tmp/hobiday-shoe-reset-release` 폴더에서 검증했다.
- 전체 ESLint·TypeScript·11개 기능 검사 통과. 격리 PostgreSQL에서 기존 권한·공개 범위·성취 계산과 함께 초기화 및 응답 유실 재시도를 검사했다.
- 비어 있지 않은 완등 기록의 점수·개수·기간 보존, 새 선택일·만료일, 실제 성취 우선, 초기화 1회 제한과 만료 후 거절 검증.
- 웹 빌드·네이티브 정적 빌드 각 42개 경로 성공, iOS/Android 동기화 완료. 1,108개 산출 텍스트 파일에 service-role JWT가 없음을 확인했다.

## 운영 반영

119개 운영 이력과 기존 함수 본문을 비교한 뒤 신규 `20260910060000_shoe_start_reset.sql` 및 이력을 한 트랜잭션으로 적용했다. 적용 후 120개 이력, 초기화·조회 함수 본문/권한, 새 컬럼과 RLS를 검증했다. 운영 회원의 색이나 완등 기록을 테스트 목적으로 변경하지 않았다.

웹은 main 병합으로 기존 Vercel 주소에 배포한다. 설치형 앱 번들은 Codemagic에서 별도 출시한다. 관련 없는 사진 폴더·자체 평가 초안·cardnews.html·환경 파일·빌드 산출물은 포함하지 않았다.
