# 뉴스 날짜·영상 목록·장소 검색 배포 — 2026-09-10

- 뉴스: 전체/날짜 선택, 한국 날짜별 DB 조회, 최신순 커서 페이지 넘김. 목록에서 사진·날짜를 유지하고 출처는 상세에 표시한다.
- 영상: 제목·작성자·반응과 세로 썸네일을 나란히 정리한 목록, 전체 영상/내 영상 이동.
- 홈 모임 검색: 장소명·지역·별칭으로 한정. 호스트 이름·소개글은 제외하고 사람 탭 검색은 유지한다.

기준 main은 `32cea6b`. 관련 변경을 보존 커밋으로 남기고 `/private/tmp/hobiday-news-release`에서 검증했다. 별도 작업인 `Photo/social/`, `web/public/cardnews.html`은 포함하지 않는다.

## 검증

- 전체 ESLint·TypeScript·8개 기능 검사 스크립트 통과.
- PGlite 실제 뉴스 테이블과 새 RPC로 한국 날짜 경계, 과거 기사, 동일 시각 커서, 숨김·대회 제외, 인증·권한 SQL 검사 통과.
- 웹 빌드·네이티브 정적 빌드 41개 경로 성공, iOS·Android sync 완료. 처음 샌드박스의 빌드용 포트 생성 제한으로 중단됐으며 같은 소스를 필요한 권한으로 빌드했다.
- 개발 화면에서 날짜 변경·전체 복귀·빈 날짜 및 영상 예시 목록을 검수했다. 검수용 임시 페이지는 제거했다.
- 운영 기존 115개 마이그레이션과 일치하는 것을 확인하고 새 `20260910020000_news_date_feed.sql` 및 이력을 단일 트랜잭션으로 적용했다. 적용 후 116개 이력과 함수 본문·security definer·search_path·인증 역할 권한이 일치한다.

GitHub main 병합으로 기존 Vercel 운영 웹 https://hobiday-eight.vercel.app/ 에 배포한다. 스토어 앱 서명·제출은 Codemagic에서 별도로 진행한다.
