# HobiDay 공통 작업 기준

Codex와 Claude 모두 이 문서와 작업 폴더의 AGENTS.md를 읽고 같은 기준으로 작업한다.
현재 기능은 `docs/current-product.md`, 통합 점검 및 DB 이력 복구 절차는
`docs/integration-audit-2026-09-08.md`를 참고한다. `PRODUCT.md` 하단과
`OPERATIONS.md`에는 폐지한 기획이 남아 있으므로 그대로 재구현하지 않는다.

## 로컬 작업과 일괄 배포

- 사용자의 기본 방식은 여러 변경을 로컬에 모아 확인한 뒤 일괄 배포하는 것이다.
- 일반 UI 수정은 개발 서버(`cd web && npm run dev`)로 확인하고, 변경에 맞는 린트·타입·기능 검사만 한다.
- 사용자가 “올리자”, “배포해줘”라고 요청하면 모아둔 변경을 검토하고 웹 빌드·네이티브 동기화를 한 번씩 수행한 뒤 푸시·배포한다.
- 로컬 미리보기에서 Supabase를 끈 경우 모임·사람 데이터는 예시다. 운영 데이터와 혼동해 완료 상태를 보고하지 않는다.

## 여러 작업을 합칠 때

- 먼저 `git status`, `git worktree list`, 로컬/원격 main의 차이를 확인한다.
- 기존 미커밋 변경을 덮어쓰거나 초기화하지 않는다. 통합 전 로컬 보존 커밋을 남기고, 관련 변경만 병합한다.
- 현재 작업 브랜치에 최신 main이 포함됐는지 확인한다. squash 병합은 커밋 해시뿐 아니라 최종 파일도 비교한다.
- `.env*`, 인증 토큰, Supabase 임시 연결 파일, 빌드 산출물을 커밋하지 않는다.

## Supabase

- 변경 SQL 작성·동작 테스트는 로컬에서 모으고, 운영 적용은 일괄 배포 단계에서 한 작업자가 담당한다.
- 원격 마이그레이션 이력·스키마·정책의 읽기 전용 조회는 상태 점검에 사용한다. 조회와 변경 적용을 구분한다.
- 이미 적용된 마이그레이션의 이름·본문을 수정하지 않는다. 새 수정은 마지막 버전보다 뒤인 새 파일로 만든다.
- `migration list`에 과거 누락이 보이면 실제 함수·테이블·정책과 먼저 비교한다.
- 과거 버전을 `--include-all`로 무조건 재실행하지 않는다. 무료 전환 뒤 예전 SQL을 실행하면 크레딧 함수가 되살아날 수 있다.
- 이력 복구는 실제 효과가 반영됐음을 확인한 버전만 `migration repair`로 표시한다.
